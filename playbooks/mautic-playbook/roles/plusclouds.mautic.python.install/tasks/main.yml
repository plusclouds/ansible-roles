---
# tasks file for plusclouds.mautic.python.install
- name: Update the system
  command: apt-get update
  become: yes

- name: Install Python
  apt: name = {{ item }} state = present
  become: yes
  with_items:
    - gcc
    - make
    - python
    - python-setuptools
    - python-pip
